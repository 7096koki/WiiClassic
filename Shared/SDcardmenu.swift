import SwiftUI

// MARK: - SDカードメニュー View
struct SDCardMenuView: View {
    var body: some View {
        Text("あとで作るから待っててぇ")
    }
}

// MARK: - Preview
struct SDCardMenuView_Previews: PreviewProvider {
    static var previews: some View {
        SDCardMenuView()
    }
}
